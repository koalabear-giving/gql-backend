export default () => {
  getFromToken: ({ token })
}

// getById
// getAll (paginated)
// get from token
// create user
// change password
// validate email ('validated') field in user database that's false by default but then becomes true once validated
